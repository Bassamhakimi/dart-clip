name: clip
description: Updated clip package with newer dependencies
version: 1.1.11  # Increment version
homepage: https://github.com/Bassamhakimi/dart-clip

environment:
  sdk: ">=3.0.0 <4.0.0"  # Updated for Dart 3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2  # Updated to match Flutter 3.35+
  photo_view: ^0.15.0
  image_picker: ^1.0.2
  flutter_cache_manager: ^3.1.3
  flutter_native_image: ^0.0.6+1

dev_dependencies:
  test: ^1.24.0  # Updated test package
